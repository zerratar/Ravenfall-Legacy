using System.Collections.Generic;
using UnityEngine;

public class MessageBubbleManager : MonoBehaviour
{
    //private readonly List<BubbleMessage> messages
    //    = new List<BubbleMessage>();

    private void Update()
    {
    }

    public void AddMessage(Transform source, string message, int duration)
    {
    }
}